export enum Role {
  owner = 'owner',
  tenant = 'tenant',
  admin = 'admin',
  staff = 'staff',
  none = 'none',
}

export enum ResultErrorEnum {
  sameHouse = 'SAME_HOUSE',
  noUser = 'USER_NOT_FOUND',
}
